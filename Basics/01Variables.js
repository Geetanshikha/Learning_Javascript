var fullname ="Geetanshikha Gautam";
console.log(fullname);

var full_name ="Geetansh";
console.log(full_name);

var _fullname ="Geetanshikha Gautam";
console.log(_fullname);

// so this is how we have declared a variable and store the string data into it
var coursename = "Javascript";
console.log(coursename);

//now we will deal with boolean data
var isLoggedin = true;
console.log(isLoggedin);

var isLoggedin = false;
console.log(isLoggedin);

//now we will deal with numerical data

var loggedcount=34;
console.log(loggedcount);
//if we are not using double comas the output will be displayed
//but if we will use double commas then the same thing will be displayed ok

var loggedcount="34";
console.log("loggedcount");

var paymode;
console.log(paymode);
//here the output will come undefined because no value is give to variable paymode.

paymode="credit card";
console.log(paymode);
//so value is given nhence output is displayed

// var if=23;
//not allowed to keep the name as if becoz it is a reserved keyword !